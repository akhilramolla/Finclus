import {Shell} from "@/components/presenter/Shell"; import {facts} from "@/content/facts";
export default function Facts(){return <Shell><h1 className="text-3xl font-bold">Fact registry</h1><p className="mt-2 text-slate-600">Every sourced claim used on stage. Click a fact anywhere to open provenance.</p><div className="mt-6 grid gap-3 md:grid-cols-2">{facts.map(f=><div className="card p-4" key={f.id}><div className="flex items-center justify-between gap-3"><b>{f.label}</b><span className={`rounded-full px-2 py-1 text-xs ${f.confidence==='verified'?"bg-emerald-50 text-emerald-700":f.confidence==='illustrative'?"bg-amber-50 text-amber-700":"bg-slate-100 text-slate-600"}`}>{f.confidence}</span></div><p className="mt-2 text-sm leading-6">{f.value}</p><p className="mt-2 text-xs text-slate-500">{f.sourceName} · {f.asOf}</p></div>)}</div></Shell>}
