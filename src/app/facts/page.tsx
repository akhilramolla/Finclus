import Link from "next/link";
import { Shell } from "@/components/presenter/Shell";
import { facts } from "@/content/facts";

const badge: Record<string, string> = { verified: "bg-emerald-50 text-emerald-700", illustrative: "bg-amber-50 text-amber-700", simulated: "bg-slate-100 text-slate-600", unconfirmed: "bg-white text-red-700 ring-1 ring-red-300" };

export default function Facts() { return <Shell><div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><div className="text-xs font-bold uppercase tracking-[.22em] text-emerald-700">Governance surface</div><h1 className="mt-2 text-3xl font-bold">Fact registry</h1><p className="mt-2 max-w-3xl text-slate-600">Every sourced claim used on stage. Confidence is visible before a challenge arrives.</p></div><Link href="/runsheet" className="rounded border border-slate-300 bg-white px-4 py-2 text-sm font-bold">Back to run sheet</Link></div><div className="grid gap-3 md:grid-cols-2">{facts.map((fact) => <article className="card p-5" key={fact.id}><div className="flex items-start justify-between gap-4"><div><div className="font-bold text-slate-900">{fact.label}</div><div className="mt-1 font-mono text-[11px] text-slate-400">{fact.id}</div></div><span className={`shrink-0 rounded-full px-2 py-1 text-xs font-bold ${badge[fact.confidence] ?? badge.unconfirmed}`}>{fact.confidence}</span></div><p className="mt-4 text-sm leading-6 text-slate-700">{fact.value}</p><div className="mt-4 border-t border-slate-100 pt-3 text-xs leading-5 text-slate-500"><div><b>Source:</b> {fact.sourceName}</div><div><b>As of:</b> {fact.asOf}</div>{fact.note && <div className="mt-1 text-amber-700"><b>Note:</b> {fact.note}</div>}</div></article>)}</div></div></Shell>; }
