"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowRight, BarChart3, FileCheck2, Gauge, Scale, Table2 } from "lucide-react";
import { Fact } from "@/components/governance/Fact";
import { AgentPanel } from "@/components/bank/AgentPanel";
import { CamDocument } from "@/components/bank/CamDocument";
import { FacilitySlider } from "@/components/bank/FacilitySlider";
import { StressPanel } from "@/components/bank/StressPanel";
import { Shell } from "@/components/presenter/Shell";
import { anchorCase } from "@/content/case";
import { baseCredit } from "@/lib/credit";

type Tab = "Evidence" | "Financials" | "Stress" | "Committee" | "CAM";
const tabs: Array<[Tab, typeof FileCheck2]> = [["Evidence", FileCheck2], ["Financials", Table2], ["Stress", Gauge], ["Committee", Scale], ["CAM", BarChart3]];

export default function Appraisal03417Page() {
  const [tab, setTab] = useState<Tab>("Evidence");
  return <Shell><div className="space-y-5">
    <div className="flex flex-wrap items-end justify-between gap-4"><div><div className="text-xs font-bold uppercase tracking-[.22em] accent-bank">Bank / credit appraisal</div><h1 className="mt-2 text-3xl font-bold tracking-tight sm:text-4xl">Credit workbench · `03417`</h1><p className="mt-2 max-w-3xl text-slate-600">{anchorCase.entity} · a five-tab decision surface for evidence, repayment capacity, downside and authority.</p></div><Link href="/bank/conflict/03417" className="inline-flex items-center gap-2 rounded border border-amber-300 bg-amber-50 px-4 py-2 text-sm font-bold text-amber-900">Open unresolved conflict <ArrowRight size={16} /></Link></div>
    <GrammarStrip />
    <div className="card overflow-hidden"><div className="flex flex-wrap gap-1 border-b border-slate-200 bg-white p-2">{tabs.map(([id, Icon]) => <button key={id} onClick={() => setTab(id)} className={`inline-flex items-center gap-2 rounded px-4 py-3 text-sm font-bold ${tab === id ? "bg-blue-50 text-blue-800" : "text-slate-600 hover:bg-slate-50"}`}><Icon size={16} />{id}</button>)}</div><div className="p-4 sm:p-5">{tab === "Evidence" && <EvidenceTab />}{tab === "Financials" && <FinancialsTab />}{tab === "Stress" && <StressPanel />}{tab === "Committee" && <CommitteeTab />}{tab === "CAM" && <CamDocument />}</div></div>
  </div></Shell>;
}

function EvidenceTab() {
  return <div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><div className="text-xs font-bold uppercase tracking-widest text-emerald-700">Evidence tab</div><h2 className="mt-1 text-2xl font-bold">Claims with a visible route to source</h2></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">3 evidence-backed dimensions</span></div><div className="grid gap-3 md:grid-cols-3">{[["Promoter", "8.2 / 10", "K. Venkataramana · RM interview"], ["Business", "7.8 / 10", "Procurement ledger · 312 suppliers"], ["Project", "8.1 / 10", "DPR + site note"]].map(([label, score, source]) => <button key={label} onClick={() => undefined} className="card p-4 text-left hover:border-emerald-400"><div className="text-xs font-bold uppercase tracking-widest text-slate-500">{label}</div><div className="mt-3 tabnums text-3xl font-bold text-slate-950">{score}</div><div className="mt-2 text-xs text-slate-600"><span className="status-dot sim-dot" />Simulated · {source}</div></button>)}</div><div className="grid gap-3 lg:grid-cols-2"><div className="rounded border border-amber-300 bg-amber-50 p-4 text-sm leading-6 text-amber-950"><b>Evidence discipline</b><br />Turnover is not scored into a clean claim. Audited financials, GSTN and AA-derived bank credits remain a credit-review conflict.</div><div className="rounded border border-slate-200 bg-slate-50 p-4 text-sm leading-6 text-slate-700"><b className="text-slate-950">Next human action</b><br />Open the conflict screen, then challenge the stale land-use assertion before sanction routing.</div></div></div>;
}

function FinancialsTab() {
  return <div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><div className="text-xs font-bold uppercase tracking-widest text-blue-700">Financial spread</div><h2 className="mt-1 text-2xl font-bold">Projection engine, not a screenshot</h2></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">7-year projection · simulated case</span></div><div className="grid gap-3 sm:grid-cols-4"><Stat label="Minimum DSCR" value={`${baseCredit.minDscr.toFixed(2)}×`} /><Stat label="Average DSCR" value={`${baseCredit.averageDscr.toFixed(2)}×`} /><Stat label="D:E" value={`${baseCredit.debtEquity.toFixed(2)}×`} /><Stat label="Promoter margin" value={`${baseCredit.promoterMargin.toFixed(1)}%`} /></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-right text-sm"><thead className="border-b border-slate-300 text-[10px] uppercase tracking-widest text-slate-500"><tr><th className="px-2 py-3 text-left">Period</th><th className="px-2 py-3">Revenue</th><th className="px-2 py-3">EBITDA %</th><th className="px-2 py-3">EBITDA</th><th className="px-2 py-3">Interest</th><th className="px-2 py-3">Debt service</th><th className="px-2 py-3">DSCR</th></tr></thead><tbody><tr className="border-b border-slate-100"><th className="px-2 py-2 text-left">FY23 <span className="text-[10px] font-normal text-slate-500">SIMULATED</span></th><td className="tabnums px-2 py-2">₹5.80 Cr</td><td className="tabnums px-2 py-2">15.2%</td><td className="tabnums px-2 py-2">₹0.88 Cr</td><td className="px-2 py-2">—</td><td className="px-2 py-2">—</td><td className="px-2 py-2">—</td></tr><tr className="border-b border-slate-100"><th className="px-2 py-2 text-left">FY24 <span className="text-[10px] font-normal text-slate-500">SIMULATED</span></th><td className="tabnums px-2 py-2">₹6.35 Cr</td><td className="tabnums px-2 py-2">16.1%</td><td className="tabnums px-2 py-2">₹1.02 Cr</td><td className="px-2 py-2">—</td><td className="px-2 py-2">—</td><td className="px-2 py-2">—</td></tr><tr className="border-b border-slate-100"><th className="px-2 py-2 text-left">FY25 <span className="text-[10px] font-normal text-slate-500">SOURCE CONFLICT</span></th><td className="tabnums px-2 py-2">₹6.90 Cr</td><td className="tabnums px-2 py-2">16.4%</td><td className="tabnums px-2 py-2">₹1.13 Cr</td><td className="px-2 py-2">—</td><td className="px-2 py-2">—</td><td className="px-2 py-2">—</td></tr>{baseCredit.years.map(year => <tr key={year.year} className="border-b border-slate-100"><th className="px-2 py-2 text-left font-semibold">Year {year.year}</th><td className="tabnums px-2 py-2">₹{year.revenueCr.toFixed(2)} Cr</td><td className="tabnums px-2 py-2">{(year.ebitdaMargin * 100).toFixed(1)}%</td><td className="tabnums px-2 py-2">₹{year.ebitdaCr.toFixed(2)} Cr</td><td className="tabnums px-2 py-2">₹{year.interestCr.toFixed(2)} Cr</td><td className="tabnums px-2 py-2">₹{year.debtServiceCr.toFixed(2)} Cr</td><td className="tabnums px-2 py-2 font-bold text-emerald-700">{year.dscr.toFixed(2)}×</td></tr>)}</tbody></table></div><div className="grid gap-3 lg:grid-cols-2"><div className="rounded border border-slate-200 bg-slate-50 p-4 text-sm leading-6"><b>MPBF-style view</b><br />Operating current assets less assumed current liabilities are converted into a working-capital gap. The WC facility is shown separately from the term facility; officer validation of the cycle remains open.</div><div className="rounded border border-blue-200 bg-blue-50 p-4 text-sm leading-6 text-blue-950"><b>Policy floor</b><br />Minimum DSCR is compared to the internal policy floor of 1.25×. The number is a case-policy input, not a regulatory claim.</div></div><FacilitySlider /></div>;
}

function CommitteeTab() { return <div className="space-y-5"><AgentPanel /><div className="grid gap-3 lg:grid-cols-3"><Unresolved title="Turnover reconciliation" owner="Credit review" /><Unresolved title="Raw-material escalation clause" owner="Condition precedent" /><Unresolved title="CEFPPC sanction letter" owner="CP before disbursement" /></div><div className="rounded border border-teal-200 bg-teal-50 p-4 text-sm text-teal-950"><b>Human authority boundary</b><br />AI recommends. The human decides. <Fact id="rbi.dl2025.limit" /></div></div>; }
function Unresolved({ title, owner }: { title: string; owner: string }) { return <div className="rounded border border-amber-200 bg-amber-50 p-4"><div className="text-xs font-bold uppercase tracking-widest text-amber-800">Unresolved</div><div className="mt-2 font-bold text-amber-950">{title}</div><div className="mt-2 text-xs font-semibold text-amber-800">NEXT · {owner}</div></div>; }
function Stat({ label, value }: { label: string; value: string }) { return <div className="rounded border border-slate-200 p-3"><div className="text-[10px] font-bold uppercase tracking-widest text-slate-500">{label}</div><div className="mt-1 tabnums text-xl font-bold">{value}</div></div>; }
function GrammarStrip() { return <div className="grid gap-px overflow-hidden rounded border border-slate-200 bg-slate-200 sm:grid-cols-4"><div className="bg-blue-50 p-3"><div className="text-[10px] font-bold uppercase tracking-widest text-blue-700">Bank system</div><div className="mt-1 text-sm font-semibold">Owns application state</div></div><div className="bg-violet-50 p-3"><div className="text-[10px] font-bold uppercase tracking-widest text-violet-700">Intelligence fabric</div><div className="mt-1 text-sm font-semibold">Calculates and flags</div></div><div className="bg-teal-50 p-3"><div className="text-[10px] font-bold uppercase tracking-widest text-teal-700">Human authority</div><div className="mt-1 text-sm font-semibold">Challenges and decides</div></div><div className="bg-emerald-50 p-3"><div className="text-[10px] font-bold uppercase tracking-widest text-emerald-700">Evidence</div><div className="mt-1 text-sm font-semibold">Source + artefact + time</div></div></div>; }
