"use client";
import Link from "next/link"; import {Play, RotateCcw, Keyboard} from "lucide-react"; import {runsheet} from "@/content/runsheet"; import {Shell} from "@/components/presenter/Shell"; import {useDemoStore} from "@/lib/store";
export default function Home(){const set=useDemoStore(s=>s.set); const reset=useDemoStore(s=>s.reset); return <Shell><div className="grid gap-5 lg:grid-cols-[1.3fr_.7fr]"><section className="card p-7 sm:p-10"><div className="text-xs font-bold uppercase tracking-[.25em] text-violet-700">Presenter control room · 60 minutes</div><h1 className="mt-3 max-w-3xl text-4xl font-bold tracking-tight sm:text-6xl">Finclus <span className="text-violet-700">—</span><br/>Intelligence Fabric</h1><p className="mt-5 max-w-2xl text-lg text-slate-600">A deterministic, evidence-backed intelligence layer across the Indian lending lifecycle. One synthetic borrower. One continuous story. Human authority stays visible.</p><div className="mt-8 flex flex-wrap gap-3"><button onClick={()=>set({started:true,current:1})} className="inline-flex items-center gap-2 rounded bg-blue-700 px-5 py-3 font-bold text-white hover:bg-blue-800"><Play size={18}/> Begin</button><button onClick={reset} className="inline-flex items-center gap-2 rounded border border-slate-300 px-5 py-3 font-bold"><RotateCcw size={18}/> Reset</button><Link href="/facts" className="rounded border border-slate-300 px-5 py-3 font-bold">Fact registry</Link></div><div className="mt-10 grid gap-3 sm:grid-cols-3"><div className="rounded bg-blue-50 p-4"><b className="text-blue-700">Systems</b><p className="mt-1 text-sm">LOS · CBS · LMS</p></div><div className="rounded bg-violet-50 p-4"><b className="text-violet-700">Fabric</b><p className="mt-1 text-sm">Evidence · agents · research</p></div><div className="rounded bg-teal-50 p-4"><b className="text-teal-700">Authority</b><p className="mt-1 text-sm">Human decides</p></div></div></section><section className="card p-6"><div className="flex items-center gap-2 text-sm font-bold"><Keyboard size={17}/> Keyboard-first</div><div className="mt-4 grid grid-cols-2 gap-2 text-sm text-slate-600">{[["→ / Space","Next"],["←","Back"],["G","Governance"],["P","Notes"],["R","Reset"],["S","Scale"]].map(([a,b])=><div key={a} className="rounded bg-slate-50 p-3"><kbd className="font-bold text-slate-900">{a}</kbd><span className="ml-2">{b}</span></div>)}</div><h2 className="mt-7 border-t border-slate-200 pt-5 font-bold">Run sheet</h2><div className="mt-3 space-y-1">{runsheet.map((x,i)=><Link href={x.route} key={x.id} onClick={()=>set({current:i})} className="flex justify-between rounded px-2 py-1 text-sm hover:bg-blue-50"><span><span className="mr-2 tabnums text-slate-400">{String(i+1).padStart(2,"0")}</span>{x.title}</span><span className="tabnums text-xs text-slate-400">{x.minuteStart}–{x.minuteEnd}</span></Link>)}</div></section></div></Shell>}
